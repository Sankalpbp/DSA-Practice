1. Two pointer approach
2. Finding the start of the loop in a linked list that ends in a loop
3. Reverse a linked list without recursion
4. Reverse a linked list with recursion
5. Merging two linked lists - recursive and iterative
6. Reverse by pairs - recursive and iterative
7. Check if a Linked list is a palindrome
8. Convert a binary tree to doubly linked list ( which follows inorder inorder traversal )
9. Quick Sort
